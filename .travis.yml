language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t jaikrish/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jaikrish/docker-react npm run test
